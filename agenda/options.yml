#Config
program:
    #Las posibles opciones son: memory, database
    run_as: 'database'
databases:
    #Los posibles tipos son: sqlite, mysql
    type: 'mysql'
    limit: 10
    sqlite3:
        name: 'agenda.db'
    mysql:
        host: localhost
        user: root
        pass: root
        schema: python_agenda

