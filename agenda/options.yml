#Config
program:
    #Las posibles opciones son: memory, database, gtk
    run_as: 'gtk'
databases:
    #Los posibles tipos son: sqlite, mysql
    type: 'sqlite'
    limit: 10
    sqlite3:
        name: 'agenda.db'
    mysql:
        host: localhost
        user: root
        pass: root
        schema: python_agenda

